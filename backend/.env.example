# Server
PORT=5000

# MongoDB
MONGO_URI=mongodb+srv://aayushmakafle:<ak123456>@cluster0.uzetg1v.mongodb.net/?appName=Cluster0

# Auth
JWT_SECRET=change_this_to_a_long_random_string

# Blockchain (fill these in after `npm run deploy:local` in /blockchain)
RPC_URL=http://127.0.0.1:8545
CONTRACT_ADDRESS=
# Private key of the Hardhat account the backend uses to relay tx (dev only!)
SERVER_WALLET_PRIVATE_KEY=
